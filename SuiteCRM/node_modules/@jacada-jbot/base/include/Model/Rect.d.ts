/**
 * TODO RONA: only Win uses this, should we remove it?
 * This class represents a rectangle, it has: 
 * left, top, width, height
 */
export default interface Rect {
    Left: number;
    Top: number;
    Width: number;
    Height: number;

    Contains(x: number, y:number);
}